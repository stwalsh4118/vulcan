// Package backend defines the common interface that all isolation backends
// (Firecracker, V8 isolates, gVisor) must implement, along with the domain
// types exchanged between the execution engine and backend implementations.
package backend
